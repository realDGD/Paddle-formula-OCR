# CUDA 11.8 runtime. Install only inside its isolated environment.
paddlepaddle-gpu==3.3.1
paddleocr==3.7.0
paddlex==3.7.2
# PP-FormulaNet_plus formula decoding requires these PaddleX optional dependencies.
tokenizers>=0.19
ftfy>=6.0
